# JetBrains tests disabled - requires Cline-specific secrets
name: Trigger Jetbrains Plugin <-> Cline Tests
on:
  pull_request_target:
    types: [opened, reopened]
  issue_comment:
    types: [created]

# DISABLED: This workflow requires CLINE_JETBRAINS_WORKFLOW_KEY secret
# which is only available in the original Cline repository

concurrency:
  group: jetbrains-trigger-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  trigger-integration-test:
    name: Run Tests (Disabled)
    runs-on: ubuntu-latest
    if: false  # Disabled - requires secrets not available in fork
    steps:
      - name: Disabled
        run: echo "This workflow is disabled in NexusAI fork"
